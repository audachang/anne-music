# Anne 暑期管樂營清單 — 自動維護任務

你是執行例行維護的 agent。今日日期為 **{{TODAY}}** (Asia/Taipei)。

## 流程

1. 讀 `prompts/sop.md` 完整 SOP
2. 讀 `prompts/output_contract.md` 嚴格遵守寫入規則
3. 讀 `data/state.json` 了解當前狀態
4. 依 SOP 第 3 節 Step B–E 執行:
   - 重跑 SOP 第 3 節列出的 WebSearch queries
   - WebFetch 候選 URL 驗證
   - 對既有 included entry 重新驗證 (URL 仍在線、deadline 未過)
   - 將過期項目移到 `excluded`
   - 將 pending 中已取得日期者升級到 `included_*`
   - 對所有變動更新 `last_changed = {{TODAY}}`
5. **唯一的寫入動作:Edit/Write 更新 `data/state.json`**
6. 確認 JSON 有效後結束 (不要試圖跑 git 或 render.py — refresh.sh 會接手)

## 限制

- **不要編輯 docs/、prompts/、scripts/、.github/、README.md、本檔**
- **不要 commit 或 push** (refresh.sh 處理)
- **不要安裝套件** (環境已備好)
- **不要呼叫破壞性 bash** (rm、git reset 等)
- 工具呼叫上限:約 30 次以內;超過代表查詢策略需精簡

## 完成標準

`data/state.json` 仍是有效 JSON,且:
- `last_updated == "{{TODAY}}"`
- 任何新增/變動的 entry 其 `last_changed == "{{TODAY}}"`
- 過期的 included entry 已移到 excluded
- 沒有觸碰任何其他檔案

完成後輸出一行摘要:`SUMMARY: +N新增 / ~M更新 / -K移除`,然後結束。
